# Deep Backlog

Epics and future themes live here. Promote work to ROADMAP.md as scoped M-### milestones — never as raw epics in ROADMAP `##` slots.

## Future themes

- **Stack-specific skill packs** — optional skills under `docs/developer/skills/` for APEX, Node, Python, etc.
- **GitHub Actions CI** — wire `validate-governance-structure.sh` and `sync-skill-stubs.sh --check`
- **Template update merge tool** — helper to pull `project-bootstrap` updates into an existing solution repo
- **Multi-solution org mode** — shared manifest patterns for monorepo consumers
