# Release archive

Completed milestones are archived here as semantic-version release notes (`vX.Y.Z.md`).

Each release closes a sprint on `main` and triggers `publish-to-install.sh` to update the consumer-facing `install` branch.
